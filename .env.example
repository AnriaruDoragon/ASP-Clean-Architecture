# ============================================================
# Docker Development (docker compose up)
# ============================================================

# Database (PostgreSQL 18)
POSTGRES_DB=aspcleanarchitecture
POSTGRES_USER=postgres
POSTGRES_PASSWORD=dev-password-123

# Exposed ports (dev only - change if ports conflict)
DB_PORT=5432
REDIS_PORT=6379

# Custom domain for Traefik (requires hosts file entry and matching certs)
API_DOMAIN=api.aspclean.localhost

# JWT Authentication
JWT_SECRET_KEY=YourSuperSecretKeyForDevelopmentOnly123!
JWT_ISSUER=ASPCleanArchitecture
JWT_AUDIENCE=ASPCleanArchitecture
JWT_ACCESS_TOKEN_EXPIRATION_MINUTES=15
JWT_REFRESH_TOKEN_EXPIRATION_DAYS=7

# Caching
CACHING_ENABLED=true
CACHING_PROVIDER=Redis

# ============================================================
# Local .NET Development (dotnet run / task watch)
# Loaded automatically via DotNetEnv - uncomment to enable
# ============================================================

# ASPNETCORE_ENVIRONMENT=Development
# ASPNETCORE_URLS=http://localhost:5141
# CONNECTIONSTRINGS__DEFAULTCONNECTION=Host=localhost;Database=aspcleanarchitecture;Username=postgres;Password=dev-password-123
# JWT__SECRETKEY=YourSuperSecretKeyForDevelopmentOnly123!
# JWT__ISSUER=ASPCleanArchitecture
# JWT__AUDIENCE=ASPCleanArchitecture
# CACHING__ENABLED=true
# CACHING__PROVIDER=Redis
# CACHING__REDIS__CONNECTIONSTRING=localhost:6379

# ============================================================
# Production Settings
# Override these values for production deployment
# ============================================================

# ASPNETCORE_ENVIRONMENT=Production
# POSTGRES_PASSWORD=<secure-production-password>
# JWT_SECRET_KEY=<secure-production-key-at-least-32-chars>
# CORS_ALLOWED_ORIGINS=https://yourdomain.com,https://*.yourdomain.com

# ============================================================
# Optional Settings (uncomment and configure as needed)
# ============================================================

# Security
# SECURITY__ENFORCEHTTPS=true

# CORS (comma-separated origins, supports wildcards)
# CORS_ALLOWED_ORIGINS=https://yourdomain.com,https://*.yourdomain.com

# Rate Limiting
# RATELIMITING__GLOBAL__LIMIT=100
# RATELIMITING__GLOBAL__WINDOW=60
# RATELIMITING__POLICIES__AUTH__LIMIT=5
# RATELIMITING__POLICIES__AUTH__WINDOW=60
# RATELIMITING__POLICIES__API__LIMIT=30
# RATELIMITING__POLICIES__API__WINDOW=60

# Graceful Shutdown
# GRACEFULSHUTDOWN__TIMEOUTSECONDS=30

# Email (SMTP)
# EMAIL__ENABLED=false
# EMAIL__HOST=smtp.example.com
# EMAIL__PORT=587
# EMAIL__USESSL=true
# EMAIL__USERNAME=
# EMAIL__PASSWORD=
# EMAIL__FROMADDRESS=noreply@example.com
# EMAIL__FROMNAME=Application
